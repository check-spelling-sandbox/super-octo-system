name: CI
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sudo apt-get install -y cpanminus
          file /usr/bin/cpanm
          : '   Text::Hunspell'
          /usr/bin/cpanm --verbose --notest Capture::Tiny Devel::Cover Devel::Cover::Report::Html Hash::Merge Module::Build::Tiny
